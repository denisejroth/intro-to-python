# Module 3 — Control Flow

Making decisions and repeating work.

**Slides:** `if`/`elif`/`else`, comparison operators, `for` loops, `range()`, `enumerate()`, and
a from-scratch word counter.

**Notebooks:** 3 exercises (+ solutions).
