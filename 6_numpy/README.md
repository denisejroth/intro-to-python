# Module 6 — NumPy

The foundation of numerical computing in Python.

**Slides:** arrays, vectorized math, summaries (`sum`, `mean`, `std`), and boolean filtering.

**Notebooks:** 3 exercises (+ solutions).
