# Module 7 — pandas

Working with real data tables.

**Slides:** DataFrames, loading CSVs, exploring (`head`, `info`, `describe`), selecting,
filtering, new columns, `groupby`, and quick plots.

**Notebooks:** 4 exercises (+ solutions).
